import 'reflect-metadata'
import "./index.scss"
import { createEditor, Editor } from '@textbus/editor'

// const json = require('./content.json')
// console.log(json)

const editor = new Editor(document.getElementById('box')!)
